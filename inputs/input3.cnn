int main() {
    int x;
    int y;
    int z;
    x = 10;
    y = 3;
    z = (x > y) ? (x + y) : (x - y);
    printf(z);

    x += 5;
    printf(x);

    y *= 2;
    printf(y);

    x /= 3;
    printf(x);

    y -= 1;
    printf(y);

    return 0;
}
