int main() {
    int x;
    int y;
    int z;
    x = 10;
    y = 3;
    if (x > y) {
        z = x + y;
    } else {
        z = x - y;
    }
    printf(z);

    x = x + 5;
    printf(x);

    y = y * 2;
    printf(y);

    x = x / 3;
    printf(x);

    y = y - 1;
    printf(y);

    return 0;
}
