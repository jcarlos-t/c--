int main() {
    int x;
    int y;
    int z;
    x = 10;
    y = 3;
    z = (x > y) ? (x + y) : (x - y);
    print(z);

    x += 5;
    print(x);

    y *= 2;
    print(y);

    x /= 3;
    print(x);

    y -= 1;
    print(y);

    return 0;
}
