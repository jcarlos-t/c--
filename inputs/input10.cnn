int main() {
    int result;
    int a;
    int b;
    a = 7;
    b = 3;

    result = (a + b) * (a - b);
    printf(result);

    a = a + b;
    printf(a);

    printf(b);

    if ((a != b) && (b > 0)) {
        printf(1);
    } else {
        printf(0);
    }

    return 0;
}
