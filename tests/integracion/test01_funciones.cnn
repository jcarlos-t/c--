// Test 01: Funciones y recursividad
int triple(int x) {
    return 3 * x;
}

int factorial(int x) {
    if (x == 0) { return 1; }
    return factorial(x - 1) * x;
}

int suma(int a, int b) {
    return a + b;
}

int main() {
    int a = 2 * 2;
    printf("%d\n", a); // esperado:4
    printf("%d\n", factorial(a)); // esperado:24
    printf("%d\n", triple(a)); // esperado:12
    printf("%d\n", suma(a,a)); // esperado:8
    return 0;
}
